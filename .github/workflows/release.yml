name: Release
on:
  push:
    branches:
    - main
  workflow_dispatch:
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: Create release draft
      uses: darioblanco/release-wizard@main
      with:
        draft: true
        token: ${{ github.token }}
        withV: true
        pushTag: false
