################################################################################
#                     DO NOT EDIT THIS FILE (Auto-Generated)                   #
# Contents of this file were generated by the central configuration repository #
# Changes to this file may be overwritten                                      #
################################################################################
name: ~Lib / Release
on:
  workflow_call:
    inputs:
      targetBranch:
        required: false
        description: The target branch for the release being created
        default: main
        type: string
      prerelease:
        required: false
        description: Defines if the release to create is a prerelease. Defaults to false
        default: false
        type: boolean
      publish:
        required: false
        description: Defines if the release can be published. Defaults to false
        default: false
        type: boolean
jobs:
  release:
    permissions:
      contents: write  # for release-drafter/release-drafter to create a github release
      pull-requests: write  # for release-drafter/release-drafter to add label to PR
    runs-on: ubuntu-latest
    steps:
    - name: Create Github release
      uses: release-drafter/release-drafter@v5
      with:
        commitish: ${{ inputs.targetBranch }}
        config-name: workflows/configs/release-drafter.yaml
        prerelease: ${{ inputs.prerelease }}
        publish: ${{ inputs.publish }}
      env:
        GITHUB_TOKEN: ${{ secrets.REPO_ACCESS_TOKEN_OPEN_SOURCE }}
